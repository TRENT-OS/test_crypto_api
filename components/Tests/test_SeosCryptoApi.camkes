/**
 * Copyright (C) 2019, Hensoldt Cyber GmbH
 */

import <seos_if_cryptoApi.camkes>;

import "../../components/CryptoRpcServer/CryptoRpcServer.camkes";

component test_SeosCryptoApi {
    control;

    // SeosCryptoRpc Interface
    dataport    Buf                         SeosCryptoDataport;
    uses        seos_if_cryptoApi           SeosCryptoRpc_Server;

    // Interface to RPC component
    uses        CryptoRpcServerInf          CryptoRpcServer;
}